config:
  service: service-name
  region: ap-southeast-1
  userpool_authorizer_arn: ARN
  user_pool_id: POOL_ID
env:
  MONGODB_ATLAS_CLUSTER_URI: mongodb://localhost:27017
  BUCKET_NAME: bkt-development
  BUCKET_REGION: ap-southeast-1
